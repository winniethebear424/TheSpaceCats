
# Loads a trained checkpoint and runs test-set evaluation.
# Saves:  test_metrics.json  (SSIM, PSNR, MSE, MAE per sample)
#         latents.npy        (latent vectors for t-SNE/KNN)
# These files are the inputs for all plotting scripts.

